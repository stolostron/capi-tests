ref:
  as: capz-test-management-cluster
  from: src
  commands: capz-test-management-cluster-commands.sh
  resources:
    requests:
      cpu: 100m
      memory: 200Mi
  timeout: 30m
  documentation: |-
    Runs CAPZ test suite Phase 03 (management cluster) to validate the
    external IPI-provisioned cluster with pre-installed CAPI/CAPZ/ASO controllers.
